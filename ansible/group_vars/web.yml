workdir: "{{ ansible_env.HOME }}/permacoop"

git_repo: "https://github.com/fairnesscoop/permacoop.git"

letsencrypt_email: "equipe@fairness.coop"

server_port: 3000

domain_name: "{{ inventory_hostname }}"
