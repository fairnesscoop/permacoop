{% extends 'layouts/app.njk' %}
{% from 'macros/table.njk' import render_table with context %}
{% from 'macros/breadcrumb.njk' import breadcrumb %}

{% block main_class %}pc-container{% endblock %}

{% block main %}
    {{ breadcrumb([{ title: 'crm-projects-title'|trans }]) }}

    <div class="pc-row pc-row--middle" style="--row-gap: calc(2 * var(--w))">
        <h1 class="pc-m" style="--m: 0">{{ 'crm-projects-title'|trans }}</h1>
        <a href="{{ path('crm_projects_add') }}" class="pc-btn">{{ 'common-add'|trans }}</a>
    </div>

    {{ render_table(table) }}
{% endblock main %}
